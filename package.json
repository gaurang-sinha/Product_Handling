{
  "name": "gaurang",
  "version": "1.0.0",
  "description": "Creating an application to manage interaction between 3 collection and CRUD over the same.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "nodemon": "^2.0.4"
  }
}
